@model IEnumerable<hrms_mvc.Models.MasterDocEmp>

<div class="main-wrapper">
    <div class="card p-4">

        <div class="d-flex justify-content-between align-items-center mb-4">

            <h3 class="mb-0">
                Employee Document Name List
            </h3>

            <a asp-controller="MasterDocEmp"
               asp-action="AddEmpDoc"
               class="btn btn-primary">
                + Add Document
            </a>

        </div>

        <div class="table-responsive">

            <table id="employeeDocumentTable"
                   class="table table-bordered table-hover">

                <thead>
                    <tr>
                        <th>Document ID</th>
                        <th>Document Name</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>

                    @foreach (var d in Model)
                    {
                        <tr>

                            <td>
                                @d.EmpId
                            </td>

                            <td>
                                @d.EmpDocName
                            </td>

                            <td>

                                <a asp-controller="MasterDocEmp"
                                   asp-action="EditEmpDoc"
                                   asp-route-id="@d.EmpId"
                                   class="btn btn-warning btn-sm">
                                    Edit
                                </a>

                                <form asp-controller="MasterDocEmp"
                                      asp-action="DeleteEmpDoc"
                                      asp-route-id="@d.EmpId"
                                      method="post"
                                      style="display:inline;">

                                    <button type="submit"
                                            class="btn btn-danger btn-sm">
                                        Delete
                                    </button>

                                </form>

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

        </div>

    </div>
</div>

@section Scripts {

    <script src="https://cdn.datatables.net/1.13.5/js/jquery.dataTables.min.js"></script>

    <script>

        $(document).ready(function() {

            $('#employeeDocumentTable').DataTable({
                pageLength: 5
            });

        });

    </script>
}